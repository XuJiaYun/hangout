<!--pages/diary/diarydetail/diarydetail.wxml-->
<view class="container">
  <image class="head-image" src="{{url}} "mode="aspectFill" wx:if="{{url.length>0}}"></image>
  <view class="author-date">
    <text class="author">{{authorName}}</text>
    <text class="const-text">发表于</text>
    <text class="date">{{diary.time}}</text>
  </view>
  <text class="title">{{diary.title}}</text>
  <!-- <view class="cu-capsule">
    <view class='cu-tag bg-red'>
      <text class='icon-likefill'></text>
    </view>
    <view class="cu-tag line-red">
      {{diary.like}}
    </view>
     <view class='cu-tag bg-blue'>
        <text class='icon-read'></text>
      </view>
      <view class="cu-tag line-blue">
        {{diary.view}}
      </view>
  </view> -->
  <view class="horizon"></view>
  <text class="detail">{{diary.content}}</text>
</view>